import { useEffect, useState } from 'react';
import IpcEvents from 'IpcEvents';
import 'tailwindcss/tailwind.css';

const DeckBuilder = () => {
  return (
    <div className="h-screen w-screen bg-slate-900 relative flex">
      Deck Builder
    </div>
  );
};

export default DeckBuilder;
